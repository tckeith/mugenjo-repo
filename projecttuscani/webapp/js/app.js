define([        /* Define modules that load into the window scope */
        'jquery',
        'bootstrap',
        'domReady',
        'moment',
        'text'
        ],
    function(
        /* Since these are loaded into the window, they are defined at the window scope and it could
         * be harmful if we define the same name. Hence the "global" prefix for the scripts that do
         * not use requirejs.
         */
    	$,
    	bootstrap,
    	domReady,
        moment,
        text
    )
    {
	
    }
);